# 隐私与数据边界

星枢OpenClaw 是 OpenClaw 与 Hermes 的本地管理面板。它会根据你启用的功能读取或写入本机配置，并可能连接你明确配置的模型服务、消息渠道、Gateway、更新源或其他第三方服务。

## 本地数据

可能保存在本机的数据包括：

- OpenClaw、Hermes 和面板配置；
- 模型服务地址、渠道配置及由用户提供的凭据；
- 会话、日志、任务、记忆和运行状态；
- 面板访问认证数据。新配置只应保存带随机盐的密码派生哈希，不应保存用户选择的明文面板密码。

具体路径和权限会因桌面版、Web 模式、操作系统及上游 OpenClaw/Hermes 版本而不同。备份前应把配置与日志视为敏感数据。

## 网络连接

以下操作可能向外部服务发送数据：

- 调用用户选择的模型服务；
- 使用 Telegram、Discord、Slack、飞书等消息渠道；
- 检查 GitHub Release、npm 官方包或下载经清单固定的运行时；
- 使用用户主动启用的浏览器、语音、媒体、Webhook 或远程 Gateway 功能。

发送范围由具体功能、服务商和用户配置决定。启用前应阅读对应第三方的隐私政策，并避免把机密数据发送给不受信任的服务。

## 访问控制

- 默认优先使用 loopback，不要直接把未初始化面板暴露到公网。
- 非 loopback 首次初始化需要一次性 setup token；初始化后该令牌失效。
- 面板认证、Gateway 认证、产品授权、恢复流程和免密访问是不同边界，不应混用。
- 通用配置 API 或 IPC 不应返回明文密码、密码哈希，也不应允许修改认证字段。

## 日志与问题报告

提交日志、截图或诊断包前，请移除 API Key、Token、Cookie、密码、聊天内容、个人标识、内网地址和文件路径中的敏感信息。不要在公开 Issue 中报告安全漏洞；请使用 `SECURITY.md` 中的私下渠道。

## 操作者责任

本仓库不能替代组织自身的数据分类、留存、备份、删除、跨境传输和合规评估。公开部署或代他人运营前，应由操作者明确数据控制者、第三方处理者和事件响应责任。
